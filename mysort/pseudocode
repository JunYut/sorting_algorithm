# mysort

int[] mysort(int arr[], int size)
{
	convert arr to string array
	sort string array base on digit_count(bytes)

	for num_string with same digit_count:
		while (digit is same)
			go to next digit
		swap

}
