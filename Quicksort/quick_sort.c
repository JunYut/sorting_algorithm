#include "quick_sort.h"

void quick_sort(int[] arr, int start, int end)
{
	// 1. set pivot
	// 2. sort elements < pivot to LHS, elements > pivot to RHS
	// 3. repeat step 1 & 2 on LHS & RHS until array size == 1
}
